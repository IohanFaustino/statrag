# B10 — Epilogue: Agentic Architecture

Synthesis: three agentic architectures, token economics, system integrity when microservices fail.

**Relevance to chat RAG**: high (philosophy).
- Pick architecture per service based on token economics + failure mode, not novelty.
- Failure-mode-driven design → matches abstract.md's "promote when quality breaks".

**Take**: codify a per-service architecture decision row in `build_instructions.md` justified by token cost + failure modes.
