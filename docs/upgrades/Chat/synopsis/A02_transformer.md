# A2 — The Transformer

Attention + self-attention, transformer architecture, masked LM, training, application.

**Relevance to chat RAG**: low for build. Useful for context-length reasoning + why long-context isn't free (attention quadratic cost).

**Take**: justifies token-budget discipline in context assembler.
