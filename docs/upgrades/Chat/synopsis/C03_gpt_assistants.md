# C3 — GPT Assistants (Lanham)

ChatGPT assistants, data-science GPT, custom actions, file-upload knowledge, publishing GPT.

**Relevance**: low for our build (we own infra).
- Custom actions pattern useful for tool-calling design.
- File-upload-as-knowledge = lightweight RAG (we already do better).

**Take**: skip platform specifics. Borrow custom-action JSON schema shape.
