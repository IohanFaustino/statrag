# A1 — Analyzing Text Data with Deep Learning

Foundations of text-for-ML. Encoding (one-hot, BoW, TF-IDF, word2vec), sequential nets (RNN/LSTM/GRU/CNN), sentiment example.

**Relevance to chat RAG**: light. Background on embeddings + similarity (props of word2vec) — useful framing for why dense retrieval works.

**Take**: skip for implementation, use to justify cosine-sim retrieval & TF-IDF as sparse baseline (compare with BM25).
