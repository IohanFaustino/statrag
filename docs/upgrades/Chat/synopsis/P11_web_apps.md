# P11 — RAG Web Apps (Polzer)

**Streamlit + Docker + AWS deploy.**

**Relevance**: low (we have FastAPI + React, no AWS planned).
- Docker recipe cross-check.

**Take**: ignore Streamlit. Borrow Docker compose hardening tips (healthchecks, env layering).
