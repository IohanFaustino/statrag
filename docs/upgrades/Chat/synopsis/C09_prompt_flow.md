# C9 — Prompt Flow (Lanham)

Microsoft **prompt flow**: automated prompt testing + evaluation harness, version prompts, regression detect.

**Relevance**: very high for testing.
- Direct input to `test_plan.md`: prompt-eff test type.
- Treat prompts as code → version + diff + eval.

**Take**: build minimal prompt-eval harness: `prompts/<mode>/v<N>.txt`, eval set, regression detection. Skip MS Prompt Flow itself.
