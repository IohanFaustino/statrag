# A10 — Building an AI Agent Application

Streamlit prototyping, caching, async, containerization (Docker), production patterns.

**Relevance to chat RAG**: medium.
- We use React+Vite SPA (web/) + FastAPI, not Streamlit. Skip Streamlit specifics.
- Caching + async patterns transferable (cache embeddings, async LLM calls).
- Docker patterns reinforce ops/docker setup.

**Take**: borrow async + cache patterns; ignore Streamlit.
