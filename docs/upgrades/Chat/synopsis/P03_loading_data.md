# P3 — Loading Data (Polzer)

Loaders: documents, databases, **images, audio, video**, multi-source ingest.

**Relevance**: low for chat (ingestion already done).
- Image-loader recipes relevant for Service 3/9 if we re-ingest captions w/ vision.

**Take**: skip for chat phase. Reference if re-ingesting figures w/ richer captions.
