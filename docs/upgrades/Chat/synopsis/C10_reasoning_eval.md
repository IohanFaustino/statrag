# C10 — Reasoning + Eval (Lanham)

**Chain of thought**, reasoning strategy eval during inference, self-eval.

**Relevance**: high.
- CoT in system prompt → required for math explainer (Service 9) + research assistant (Service 8).
- Inference-time strategy eval (self-check) → matches abstract.md "self-check pass" for quiz.

**Take**: CoT optional per mode (off for navigator, on for math/research). Self-check sub-step required for quiz answer rubric.
