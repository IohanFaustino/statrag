# P6 — Vector DBs + Similarity Search (Polzer)

DB choice, similarity metrics, indexing trade-offs.

**Relevance**: low (Qdrant already selected).
- Use HNSW param tuning recipe if retrieval p95 latency too high.

**Take**: skip selection. Use perf-tuning section if needed during load testing.
