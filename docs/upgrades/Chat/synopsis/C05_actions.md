# C5 — Empowering Agents w/ Actions (Lanham)

Agent action definition, OpenAI function calling, **Semantic Kernel** (semantic + native functions), interactive service agent, semantic service design.

**Relevance**: high.
- OpenAI function calling = our tool-calling channel for retrieval invocation.
- Semantic Kernel mix of semantic (LLM) + native (Python) functions = clean way to combine deterministic retrieval w/ LLM synthesis.

**Take**: model retrieval as a tool the LLM can call (function-calling), not just pre-injection. Useful for tutor follow-ups + navigator drill-down.
