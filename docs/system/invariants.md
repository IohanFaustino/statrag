# Invariants — verify after every code change in ingestion/retrieval

These were caught during diagnostics in steps 32-38. Any change to
`llm_enrich.py`, `regex_pass.py`, `build_documents.py`, or `pipeline.py` must
preserve all of them. Verification commands are in each row.

| # | Invariant | Verification |
|---|---|---|
| 1 | `book`, `book_slug`, `book_name` all present; `book == book_slug != book_name` | `qc.scroll(...)` → payload has all three keys |
| 2 | `page_from` is never `-1` for the first section of a chapter | Run preview with `limit_sections=1`; check `pl.get('page_from') > 0` |
| 3 | One section ≈ one chunk; multi-chunk only if section > 8000 tokens | `BuildStats.split_sections` matches sections > 8000 tokens |
| 4 | `h1` is populated even when no `# ` header exists in the slice | `chapter_title` from yaml seeds it; assert `pl['h1'] != ''` |
| 5 | `synopsis` is always `str`, never `list` | `assert isinstance(pl['synopsis'], str)` |
| 6 | `n_oversize == 0` after every ingest | Asserted in `docs/docs/notebooks/00_run_all.ipynb` cell 4 |
| 7 | `LIMIT_SECTIONS != None` MUST NOT write manifest | Pre/post `manifest.list_books()` length unchanged |
| 8 | Pydantic warning "Expected str but got list" → prompt drift | Re-check `llm_enrich.SYSTEM` rule #1 or `_coerce_synopsis` |
| 9 | `h2_path` uses ` | ` (PATH_SEP) between hierarchy levels, never `_` or `/` | `assert ' | ' in pl['h2_path']` when section has subsection |
| 10 | Numbered headers ("2.1", "2.1.1") form hierarchy backbone; unnumbered headers are leaf subsections | Inspect path: numbered prefixes appear left-most, unnumbered at the right |
| 11 | `theme` is populated on every payload (text + image) and equals the yaml value | `assert pl['theme']` non-empty; equals `src/ingestion/books/<slug>.yaml :: theme` |
| 12 | Dark theme uses strict red accent (`--accent-primary: #E5484D`); electric-blue `rgba(63,169,255,X)` is forbidden anywhere in the codebase. Light theme retains cobalt accent (`#1E3A8A`). Slider thumbs / checkboxes must use theme tokens, never default UA blue. | `grep -r "rgba(63,\s*169,\s*255" web/ src/` returns nothing; theme CSS uses `--accent-primary` tokens only |
| 13 | Every raw model JSON in `deep_tutor.py` MUST be pre-processed by `_repair_latex_escapes` before `json.loads`. Every aspect string MUST be post-processed by `_repair_latex_post` after parse. Reason: OpenAI structured-stream silently corrupts single-backslash latex commands (`\theta` → tab+`heta`). | grep `_repair_latex` near every `json.loads` call; check `_convert_to_tutor_answer` runs the post-pass loop |
| 14 | `figures_full` SSE event is emitted on EVERY tutor turn (even with empty `figures: []`). Companion `figures_meta` event carries the reason. Frontend can rely on event arrival to distinguish "no figures" from "image branch never ran". | In `deep_tutor.py::run_deep_tutor`, no `if approved_figures:` guard around `yield {"type": "figures_full", ...}`. Both events always yielded before `retrieval_meta`. |
| 15 | Image-only ingest writes a `ManifestEntry` with `chapter_id="images_only"`, `chunk_count=0`, `image_count=N`. Backfill via `ops/scripts/backfill_image_manifest.py` for legacy data. | `grep -c 'images_only' data/parsed/manifest.json` equals number of ingested image books |
| 16 | Image collections (`<field>_images`) MUST exist before any tutor query routes there; `ensure_image_collection` runs inside `_persist_images` (auto-creates). Upserts MUST be chunked at ≤ 100 points per batch to avoid Qdrant write timeouts. | Inspect `pipeline.py:_persist_images` for `BATCH = 100` and per-batch upsert loop |
| 17 | `DEEP_TUTOR_INSTRUCTIONS` consumes an OPTIONAL synthesis plan: when `<synthesis_plan>`/`<evidence_ledger>`/`<contrasts>` blocks are present the draft must follow the thesis + ledger and surface contrasts. `build_synthesis_plan` MUST stay best-effort (return `None` on failure) so the draft never blocks; the source bundle remains citation ground truth. | grep `<plan>` in `prompts/deep_tutor.py`; confirm `build_synthesis_plan` wraps the call in try/except returning `None` |
| 19 | The query planner (`extract_concepts_ex` → `QueryPlan`) and coverage check (`coverage.py`) are best-effort: planner failure → empty `queries`/`facets` (legacy single-query path); coverage failure → proceed with current sources. The **raw question is always retrieved** as the anchor pool (multi-query only *adds* + RRF-fuses); coverage re-query is **capped at 1**. Gated by `TUTOR_MULTI_QUERY` / `TUTOR_COVERAGE_CHECK` (off == legacy behavior). | grep `_rrf_merge`/`_multi_query_candidates`/`assess_coverage`; both wrapped in try/except |
| 33 | The query planner always returns a `QueryPlan` and never raises to its caller. Whether single-call or chained (`TUTOR_PLANNER_CHAIN`), failure degrades: chain → single-call → keyword heuristic. | grep `extract_concepts_chain`/`_extract_concepts_single` wrapped in try/except in `deep_tutor.py`; chain errors caught per-step before fallback |
| 34 | The orchestrator-workers harness (`TUTOR_OW_HARNESS`) never changes the answer at level 0/1 and always falls back to level 0 on any harness failure; LangSmith tracing (`maybe_traced`) is observability-only and must not alter outputs. | grep `ow_harness_level`/`maybe_traced` in `agents/ow_harness.py` (returns `fn` unchanged when off/on-failure); `on_briefs` hook wrapped in try/except in `orchestrator_workers.py` |
| 35 | **Deep-synthesis live path = L0 structured synth (no schema-fill)**: the live `orchestrator-deep` path (`tutorWorkflow="orchestrator-deep"` or `TUTOR_OW_HARNESS=5`) routes directly to the **L0 structured synthesizer** (`_stream_structured`) — it emits a typed `DeepTutorAnswer` natively via `response_format` with no `_schema_fill` re-express pass. The deepagents+skill+schema-fill path and the structured deepagents agents (levels 6/7) are eval-only, reachable only via `TUTOR_OW_HARNESS=5` (L5 deepagents+skill, kept for ablation) or `=6/7` (structured deepagents agents). With `TUTOR_OW_HARNESS=0` and `tutorWorkflow` not `"orchestrator-deep"`, tutor output is byte-for-byte the level-0 behavior; `deepagents` may not be installed. Any failure on the `orchestrator-deep` path falls back to the L0 synthesizer. | `run_orchestrator_workers` L0 synth is reached directly when `deep_synth=True` (lean path); `test_deep_synth_falls_back_to_L0_on_skill_failure` for legacy L5 fallback; `TUTOR_OW_HARNESS` in `ow_harness.py` gates levels 5/6/7 |
| 20 | Adjacent-section expansion (`density._fetch_neighbor_chunks`, gated `TUTOR_NEIGHBOR_EXPAND`=1) is **rerank-gated + sibling-prefiltered**: neighbors are added to the candidate pool as low-score `_PseudoPoint`s, kept ONLY if same parent (`_parent_section`) and within the page band; the **existing cross-encoder rerank + `final_top_n` cut is the gate** (NO tuned score threshold). The scroll is **best-effort** — any error → `[]`, never blocks selection. Neighbors enter the pool *before* the coverage check, so coverage's "try neighbors before re-query" intent is satisfied at the density stage (no separate coverage neighbor call). `TUTOR_DENSITY_ALPHA` (0.6) is the concept-TF weight in `_section_score`; `_DIVERSITY_MAX` default 6. | grep `_fetch_neighbor_chunks`/`_parent_section` in `density.py`; confirm wrapped in try/except returning `[]`; `_density_select` appends before rerank |
| 21 | The author-diversity target is honored end-to-end, NOT just at the round-robin: `_density_select` scales the section budget `eff_sections = max(top_sections, target_authors)` (you need ≥N sections to surface N authors) AND applies an **author-aware rerank floor** — after the cross-encoder cut (widened to `max(final_top_n, target_authors)`), the best dropped chunk of each missing author is re-inserted, budgeted to `target_authors - kept` so the floor never overshoots the target. Without all three (cap≥N, section budget, rerank floor) a chosen N silently collapses to ~3. Final count is still bounded by distinct authors in the candidate pool (no padding). | grep `eff_sections`/`rerank floor` in `_density_select`; `test_density_select_scales_sections_to_target` + `_corpus_limit_honest` |
| 23 | Component FORMULAS have a home in `definition`: when a concept decomposes (bias/variance), `definition` writes each component's formula + the central decomposition (e.g. MSE = bias²+variance+irreducible) inline, INCLUDING a central quantity (MSE) that appears in the sources/figure even if absent from the question — formulas are NOT deferred to `formal_statement` (which is empty without a numbered theorem). **Enforced at schema time** (2026-06-05): `DeepTutorAnswer._require_component_equations` rejects a *mathematical* answer (`math_blocks` non-empty OR `$$` in `definition`) whose any `### ` subsection lacks a real `$$…$$` block — word-form pseudo-equations (`$$\text{Squared bias}+\text{Variance}\approx\text{Test MSE}$$`) do NOT count; the `ValidationError` rides the ADR-005 repair path to backfill. **Best-effort (2026-06-05):** this check (and any future format validator) is skipped under `model_validate_json(..., context={"skip_format_checks": True})`; `_validate_and_repair`'s final fallback uses that context to RENDER a structurally-valid-but-format-imperfect answer rather than blanking the turn after the one repair fails. The `organize` drafting workflow (`TUTOR_WORKFLOW=organize` / `tutorWorkflow:"organize"`) hands a token-budgeted large pool (`_build_organize_pool`, density-ranked first, trimmed to `TUTOR_ORGANIZE_MAX_TOKENS`) to `TUTOR_ORGANIZE_MODEL` (deepseek-v4-pro) with `ORGANIZER_PREAMBLE`; it AUGMENTS (density-rank still runs → diversity + figure co-location intact) and is best-effort (organize→single fallback; router salvages partial payloads, falls back on empty). The planner always emits an application-case facet+query; `applications` cites SPECIFIC cases (method/dataset/study), never bare domain labels. | grep `formulas have a home`/`ORGANIZER_PREAMBLE`/`_build_organize_pool` ; `test_definition_formulas_have_a_home_contract`, `test_organize_workflow_resolves`, `test_build_organize_pool_token_budget`, `test_planner_emits_application_case_facet`, `test_applications_real_cases_contract`; schema-time enforcement: `test_component_equations.py` (14, incl. `test_math_definition_missing_variance_equation_rejected`, `test_pseudo_equation_rejected`, `test_noncompliant_json_triggers_repair`) |
| 24 | EVERY inline `[N]` citation marker in the rendered answer has a matching `citations` entry with `index==N` linking to a source — `_ensure_marker_citations` synthesizes any missing entry from `sources` in marker-appearance order (guards under-citing models, esp. the deepseek router). Frontend: the `[N]` pill's `onClick` opens the Sources panel + scrolls/highlights `#cite-N` regardless of URL-hash state (a repeat click fires no `hashchange`). Aspect bodies keep left-aligned `### ` (H3) SUBSECTION headers as the backbone; INSIDE each subsection the body is C-style — a short **bold** lead sentence + bold lead-in bullets (one claim per line, `[N]` at line end), with display math `$$…$$` and any `[Fn]` figure marker placed in the subsection they belong to (each Example `### Case` carries its own formula + figure). **Render hardening (2026-06-05):** `_wrap_bare_math` wraps bare math runs — including runs mixing `\commands` with unicode greek (U+0370–U+03FF) — in `$…$` so inline LaTeX never leaks as raw source; figures are deduped by `ref`/`url`, boilerplate-caption figures dropped (`_is_generic_figure`), and at most ONE `### Figure example` block per aspect (highest `judge_confidence`). Prompt rules (DELIMIT ALL MATH / INLINE-vs-DISPLAY) keep `[N]` before a display block, never dangling after. `TutorView.splitIntoBlocks` parses `### `→`h3` (kept in the section body; only `## `/h2 starts a collapsible section). | `_ensure_marker_citations` fills gaps; `test_ensure_marker_citations_fills_gaps`; `test_structure_requires_subsection_headers`; `test_math_and_figures_placed_in_subsection`; `test_synthesis_skill_requires_c_style_body`; frontend `splitIntoBlocks — §12 ### subsection headers` |
| 18 | The orchestrator-workers workflow takes its subtasks from the **Planner** (`plan.tasks` — Planner + Orchestrator are ONE agent, no second LLM call); empty → per-author `_fallback_tasks`; <2 subtasks/briefs → single draft (`run_orchestrator_workers` returns `(None, {})`). The synthesizer reuses the SAME `DeepTutorAnswer` schema + `_stream_structured` + citation rules as the single draft; both paths stream via the one `_emit_aspect_delta` queue. The modal `PipelineDiagram` redraws (Orchestrator→workers→synthesizer) when `tutorWorkflow === "orchestrator"`. Critique is opt-in (`TUTOR_DEEP_CRITIQUE=1`), not on the canonical graph. | `run_orchestrator_workers` uses `plan.tasks or _fallback_tasks(...)` then `(None, {})`; `_draft_coro` falls back to `_stream_draft` |
| 25 | Detached, resumable runs (§13): a `POST /api/chat` **with** a `conversationId` runs via `runs.start_run` as a background `asyncio.Task` whose generation + persistence complete even with **zero** subscribers (client disconnect ≠ cancel). Every SSE event carries a strictly increasing 1-based `seq` (and SSE `id:`); `GET /api/chat/{id}/stream?after=N` replays `seq>N` then streams live; `GET /api/chat/{id}/status` → `{exists,active,done,seq}`. At most ONE active run per conversation (duplicate `POST` attaches, never starts a 2nd turn). Runs are process-local + GC'd `RUN_TTL_SECONDS` (300s) after completion. Frontend (`web/src/state/chat.ts`) keeps a multi-conversation store and does NOT abort the in-flight `fetch` on conversation switch; `loadConversation` reads slice state via a ref (NOT `store.byConv` in deps) so the deep-link effect does not loop-spam `/status`. | `runs.py::_drive` always drains source + sets `done` in `finally`; `start_run` returns existing active run; `subscribe` dedups by `seq`. Tests: `test_runs.py` (8) + `web/src/state/chat.test.ts` (6). Browser-verified: reopening an active conv fires exactly one `/status` + one `/stream`. |
| 28 | **Prompt schema (Zeroth law)**: every system prompt used anywhere in `src/services/chat/` MUST be XML-tagged with at minimum `<role>` + `<context>` + `<task>`. Function-specific tags (`<rules>`, `<examples>`, `<output>`, `<structure>`, `<failure_mode>`, `<*_addendum>`) are added per the agent's role per `docs/common ground/Agents/feature_Agent.md`. Plain-text "you are X, do Y" prompts are forbidden — they silently break smaller models (Llama 4 Scout drops backslashes; coverage misses sub-tasks). When the prompt is an addendum concatenated onto a base prompt (e.g. `_GROQ_PROMPT_ADDENDUM` → `DEEP_TUTOR_INSTRUCTIONS`, `SYNTHESIZER_ADDENDUM` → same), use `<role_addendum>` / `<context_addendum>` / `<task_addendum>` / `<rules_addendum>` so the parent tags remain intact. The three QA prompts (`QA_SCOPE_PROMPT`, `QA_GENERATE_PROMPT`, `QA_VERIFY_PROMPT` in `prompts/qa.py`) are XML-scaffolded with `<role>/<task>/<output_format>/<rules>`. The qa, chapter, facilitate, and scope-resolve LLM calls route through `apply_structured_output` (per-model `json_schema` vs `json_object` + `<response_format>` hint). The tutor pipeline uses native structured output directly (`beta…parse` / `json_object`) and does NOT go through `apply_structured_output`. Adding a new per-call mode requires: XML-scaffold the prompt with at minimum `<role>/<task>/<output_format>` AND wire the call through `apply_structured_output`; adding a new tutor pipeline stage requires native structured output following the existing pattern in `deep_tutor.py`/`orchestrator_workers.py`. | Audit script: every constant named `*PROMPT*`, `*INSTRUCTIONS*`, `*PREAMBLE*`, `*ADDENDUM*`, `*CRITIQUE*` in `src/services/chat/prompts/*.py` and `src/services/chat/agents/*.py` must contain `<role>`, `<context>` (or `_addendum` variants), and `<task>`. Run the loop in `tests/test_prompt_schema.py` (if absent, the in-line snippet in `changelog.md` 2026-05-25 entry covers the same check). |
| 27 | When the deep-tutor draft model is a Groq-hosted model (membership in `GROQ_MODEL_IDS`), `_stream_draft` MUST append `_GROQ_PROMPT_ADDENDUM` to the system prompt via `_maybe_append_groq_addendum(...)`. The addendum enforces (a) display math wrapped in `$$ ... $$` on its own line, (b) double-escaped LaTeX backslashes inside JSON strings, (c) `page_from`/`page_to` as integers. **Math-lift fallback**: if the parsed `DeepTutorAnswer.math_blocks` is empty, `_finalize_tutor_answer` MUST call `_lift_math_blocks_from_text(...)` on the joined aspect text to populate `math_blocks` from any `$$...$$` or LaTeX-containing inline `$...$` formulas (up to 6, deduped). Without both, the UI formula box stays empty for Groq drafts. | grep `_maybe_append_groq_addendum` in `_stream_draft`; grep `_lift_math_blocks_from_text` in `_finalize_tutor_answer`; A/B test with Groq draft on a formula-heavy question yields `math_blocks >= 1` |
| 26 | Groq is a chat-only LLM provider mounted alongside OpenAI + DeepSeek. Routing in `src/services/chat/llm/router.py` uses **set membership** (`GROQ_MODEL_IDS`), NOT prefix matching, because Groq exposes `openai/gpt-oss-*` IDs that collide with the OpenAI prefix. `GROQ_MODEL_IDS` must equal the set of model IDs in the `groq` `ModelProvider` entry. Groq's native `response_format` (`json_object` / `json_schema`) is passed **through verbatim** — no coercion to OpenAI like DeepSeek does in `orchestrator_workers.py:171`. Reasoning models (`openai/gpt-oss-*`) may emit pre-JSON tokens that Groq's validator rejects → the orchestrator repair loop is the safety net. `ProviderId` Literal in `schemas/_core.py` AND `web/src/types.ts` must include `"groq"`. Ingestion (`src/ingestion/llm_client.py`) is OUT OF SCOPE — Groq is NOT a valid ingestion provider. | `GROQ_MODEL_IDS == {m.id for m in groq_provider.models}` (`test_groq_model_ids_match_provider_registry`); `get_llm("openai/gpt-oss-120b")` → `GroqChat` not `OpenAIChat` (`test_get_llm_groq_gpt_oss_routes_to_groq_not_openai`); `_build_response_format` in `groq_client.py` passes dict payloads through; live test `test_groq_json.py` |
| 22 | Deep-tutor answer shape is the 6-aspect set `tldr, definition, formal_statement, example_intuition, applications, further_reading` (was 7: `intuition`+`examples` are MERGED into `example_intuition`; `trade_offs` is RENAMED to `applications`). All fields stay REQUIRED in `DeepTutorAnswer` (OpenAI strict structured-output forbids optional fields). `formal_statement` is **conditional via empty string**: emitted ONLY when a source has a numbered/labelled statement (Definition X.Y.Z / Theorem / Proposition) as "Conforming to …" + verbatim blockquote, else `""` (no invented theorem) — omission is render-side, `assemble_markdown` skips empty bodies so the heading disappears. `example_intuition` follows a three-move structure (three cases → analysis → literal "The intuition here is that …"). `applications` is corpus-grounded use-cases grouped by domain. Draft creativity = `TUTOR_DEEP_TEMPERATURE` (default 0.4) on all draft paths; structured-stream carries it, `parse()` fallback omits it (reasoning-model temp-reject degrades cleanly). `ASPECT_HINT`, `image_judge` hints, `App.tsx` heading map, and the example-relevance audit (`_score_example_relevance`/`_embed_relevance` now score `example_intuition` vs definition+formal_statement) all use the 6-aspect set. | `set(DeepTutorAnswer.model_fields) ⊇ {example_intuition, applications}` and excludes `intuition/examples/trade_offs`; `ASPECT_HEADINGS["example_intuition"]=="Example & Intuition"`, `["applications"]=="Applications"`; `test_example_intuition_merged_field_contract`, `test_formal_statement_verbatim_contract`, `test_applications_grouped_by_domain_contract`, `test_aspect_heading_renamed_to_applications` |

| 29 | Q&A mode (`qa`) always emits a `QAAnswer`; a corpus miss (0 retrieved sources) yields an honest no-coverage message with empty citations — never a fabricated citation. The verify node is advisory and never suppresses the answer; both the normal and corpus-miss SSE paths emit the same event types ending in `done`. | `test_qa_run.py::test_run_qa_corpus_miss_no_fabricated_citation` asserts `citations==[]` and `"not" in text.lower()`; `test_run_qa_emits_full_event_sequence` asserts `types[-1]=="done"` on both paths. |
| 30 | Chapter modes (`facilitate`/`resume`) MUST emit `ChapterDigest.blocks` in chapter reading order (`page_from`, then `section_id`); block order equals the fetched-section order from `fetch-chapter` and is **never re-sorted downstream** — neither by the stitch node, nor by the frontend renderer, nor by any serialization step. | `test_chapter_run.py::test_blocks_order_matches_fetch_order` asserts `blocks[i].page_from <= blocks[i+1].page_from` for all `i` across a multi-section run; `ChapterDigestCard.test.tsx` asserts rendered block order matches input `blocks[]` index order. |

| 31 | **Book scope resolve**: a confident single book/chapter match (`book_confidence >= BOOK_CONFIRM_CUTOFF=0.6`, exactly one candidate) NEVER emits a `clarify` event — the pipeline runs immediately. A book that was explicitly selected by the user (e.g. by clicking a chip) is always treated as `book_confidence=1.0` and never clarifies. The `clarify` gate is advisory routing; it is disabled entirely by `CHAPTER_CLARIFY=0` (old fail-open). Applies to `facilitate`, `resume`, and `qa` modes. | `maybe_clarify` in `_scope.py` returns `None` when `book_confidence >= BOOK_CONFIRM_CUTOFF and len(book_candidates) < 2 and chapter_ok`; `test_scope_confident_match_no_clarify`, `test_scope_selected_book_always_confident`. |

| 32 | **Facilitate clarify-not-expand**: the `facilitate` body is never longer than its source section; extra detail lives only in concept anchors (`[[cN]]` modals — formulas use the same `[[cN]]` marker with `kind="formula"`; there is no separate `[[fN]]` namespace). Block order == section reading order (`page_from`). Adaptive concept sub-retrieval prefers the same author + a prior section (formal-statement boost), escalating to same-author anywhere and then to other authors only when the retrieval score is below `CONCEPT_MIN_SCORE` (0.30). The `retrieve` stage key appears in SSE progress events but never accepts a model override (embedding-only). `resume` is unaffected and continues to emit `ChapterDigest`. | `FacilitateBlock.body` token count < source section token count (enforced by teach-node prompt); `blocks[i].page_from <= blocks[i+1].page_from` for all `i`; `fetch_concept_support` policy tests in `test_facilitate_concept_map.py`. |

| 36 | Strict `json_schema` structured output (`response_format=<PydanticModel>`) is only sent to OpenAI-family models. Every draft/synthesis call routes non-OpenAI models (deepseek/qwen/gemini/groq) through the `json_object` path (`_stream_draft_via_router`); OpenAI-family models use `_stream_structured` (strict `json_schema`). The predicate `is_structured_output_capable(model_id)` in `router.py` is the single source of truth — it returns `False` for deepseek/qwen/gemini by prefix and for groq by `GROQ_MODEL_IDS` set membership — and it mirrors the provider dispatch exactly so routing and client never disagree. The default draft model `gpt-5.4-nano-2026-03-17` is OpenAI-family; qwen-plus is not (it hung under `json_schema`, driving this change). | `is_structured_output_capable` returns `False` for each non-OpenAI prefix/set; `_stream_draft` calls `_stream_draft_via_router` for non-capable ids and `_stream_structured` for capable ones; same gate in `orchestrator_workers.py` L0 synth |
| 37 | **Formula recovery is gap-triggered + best-effort**: the recovery stage in `run_orchestrator_workers` (between worker briefs and the L0 synth) NEVER changes a normal answer and NEVER raises to its caller. `detect_formula_gaps(sources, query)` is pure (no I/O); when it finds a concept whose defining equation was OCR-dropped to an image, `recover_formulas` runs per gap in parallel (`asyncio.gather(return_exceptions=True)`): global `formula_cache` lookup → vision off the figure (`search_figures` + `inspect_figure` with a transcription `instruction`) → text re-query. Recovered LaTeX is injected into the synth `user` message as a `<recovered_equations>` block (used VERBATIM per the `deep_tutor.py` rule) and write-cached. Any failure anywhere → empty block → today's behavior. The `formula_cache` Qdrant collection is auto-created on first write; lookup gates on score ≥ 0.93. | `run_orchestrator_workers` wraps the stage in try/except (`recovered_block=""` default); `detect_formula_gaps` imports only stdlib + schemas; `recover_formulas` uses `return_exceptions=True`; `cache_lookup`/`cache_write` swallow all exceptions. Tests: `test_formula_gaps.py` (6), `test_formula_cache.py` (4), `test_formula_recovery.py` (7), `test_orchestrator_workers.py::test_recovered_equations_injected_into_synth` + `test_no_recovered_equations_when_no_gaps`, `test_tutor_prompt_contract.py::test_recovered_equations_rule_present` |

## One-shot verification script

Run after any ingest:

```python
from src.core.qdrant_store import client
from src.core.config import settings
qc = client()
res, _ = qc.scroll(settings.qdrant_collection_text, limit=20, with_payload=True)
for p in res:
    pl = p.payload
    assert pl.get('book') and pl.get('book_slug') and pl.get('book_name')  # 1
    assert pl.get('book') == pl.get('book_slug') != pl.get('book_name')    # 1
    assert pl.get('page_from', -1) > 0                          # 2
    assert pl.get('h1', '') != ''                               # 4
    assert isinstance(pl.get('synopsis', ''), str)              # 5
    assert pl.get('token_count', 0) <= 8190                     # 6
    assert pl.get('theme'), 'theme missing'                     # 11
print('All invariants pass for', len(res), 'sampled points.')
```

Or via notebook cell 5 in `00_run_all.ipynb`.
