[pytest]
markers =
    integration: requires running Chroma + OPENAI_API_KEY
    quality_images: live image-judge quality eval (needs OPENAI_API_KEY + labelled CSV)
addopts = -ra -q --strict-markers -m "not quality_images"
testpaths = tests
